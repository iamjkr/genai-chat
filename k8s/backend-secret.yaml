apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: genai-chat
type: Opaque
data:
  # Base64 encoded values - replace with your actual encoded API keys
  # Example: echo -n "your-api-key" | base64
  GROQ_API_KEY: "your-base64-encoded-groq-api-key"
  # Add other API keys as needed
  # TOGETHER_API_KEY: "your-base64-encoded-together-api-key"